# 은혜의 하루 — Supabase 동기화 설정

이 폴더는 원본 `hamin_planner_ver6.3.html`을 보존한 채 만든 동기화 버전입니다.

## 지금까지 완료된 것

- 기존 브라우저 자동 저장(`localStorage`) 유지
- Supabase 이메일/비밀번호 로그인 추가
- 사용자별 `planner_data.store` JSONB 자동 저장
- 같은 계정으로 로그인한 다른 기기의 변경사항 수신
- 모바일 반응형 UI 유지
- 홈 화면에 설치 가능한 PWA 기본 파일 추가
- Supabase 프로젝트 URL과 **publishable key** 연결

브라우저 코드에는 공개용 publishable key만 들어 있습니다. `service_role` 키는 절대로 HTML에 넣지 마세요.

## 1. Supabase 데이터베이스

현재 프로젝트에는 `planner_data` 테이블과 RLS 정책이 이미 만들어져 있습니다. 실시간 동기화까지 확실히 켜려면 Supabase SQL Editor에서 `supabase-schema.sql` 전체를 한 번 실행하세요. 이 파일은 다시 실행해도 기존 테이블과 정책을 보존하도록 작성되어 있습니다.

## 2. 로그인 설정 확인

Supabase Dashboard → Authentication → Sign In / Providers에서 Email provider가 활성화되어 있는지 확인합니다.

기본 보안 설정대로 이메일 확인을 유지하는 것을 권장합니다. 처음 계정을 만들면 받은편지함의 인증 링크를 누른 뒤 플래너로 돌아와 로그인하세요.

## 3. 먼저 컴퓨터에서 시험

먼저 원본 플래너에서 `백업`을 눌러 최신 JSON 파일을 만듭니다. 그 다음 새 `index.html`을 열고:

1. 새 플래너의 `데이터 → 가져오기`로 방금 만든 JSON을 불러옵니다.
2. 기록이 정상적으로 보이는지 확인합니다.
3. 오른쪽 위 `클라우드 로그인`을 누릅니다.
4. `처음 계정 만들기`로 계정을 만듭니다.
5. 이메일 인증 후 로그인합니다.
6. 클라우드에 기존 기록이 없으면 현재 브라우저 기록이 최초 데이터로 업로드됩니다.
7. 클라우드 기록이 이미 있으면 클라우드 기록을 불러옵니다.

새 HTML 파일은 원본 HTML의 브라우저 저장공간을 자동으로 공유한다고 보장할 수 없습니다. 따라서 **첫 로그인 전에 JSON 가져오기를 먼저 하는 것**이 가장 안전합니다.

## 4. 휴대폰에서 쓰려면 HTTPS로 배포

휴대폰에서는 `file://` HTML을 직접 여는 대신 이 폴더 전체를 정적 웹 호스팅에 올려야 합니다. Netlify, Vercel, GitHub Pages 같은 HTTPS 정적 호스팅이면 됩니다.

배포 주소가 생기면 Supabase Dashboard → Authentication → URL Configuration에서:

- Site URL: 배포된 주소
- Redirect URLs: 같은 주소와 필요한 하위 경로

를 등록합니다.

그 다음 컴퓨터와 휴대폰에서 같은 배포 주소를 열고 같은 계정으로 로그인합니다.

## 5. 휴대폰 홈 화면에 설치

- Android Chrome: 메뉴 → `앱 설치` 또는 `홈 화면에 추가`
- iPhone Safari: 공유 버튼 → `홈 화면에 추가`

## 동기화 규칙

- 편집 내용은 먼저 현재 기기에 저장되고, 로그인 상태에서는 약 1초 뒤 Supabase에 저장됩니다.
- 동일한 계정을 두 기기에서 동시에 편집하면 마지막으로 저장된 전체 플래너 JSON이 우선합니다.
- `백업`/`가져오기` 기능은 그대로 사용할 수 있습니다.
